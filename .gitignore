# 本地开发与沙箱测试缓存，不属于运行时状态。
.gocache/

# IDE / 编辑器本地状态。
.idea/
.vscode/

# 临时文件与本地环境变量。
.env
*.log
*.tmp

# Go 编译产物。
*.exe
*.test
coverage.out

# 前端构建期依赖（运行时只发布编译出的 web/static/css/app.css）。
node_modules/

# Claude Code 本地配置。
.claude/

# 重构前的旧代码归档（含旧密钥，不入库）。
.deprecated/
